config I
    hex "hex"
    default 0xA

config J
    hex
    default 0x1 if B
    default 0x0

config K
    hex


config B
        bool "Provide compatible behavior for rare corner cases (bigger code)"
        default n
